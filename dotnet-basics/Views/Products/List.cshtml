@model List<Product>

<h1>Products</h1>

<div class="row">
    @foreach(Product urun in Model) {
        <div class="col-md-3">
            @await Html.PartialAsync("_ProductCard", urun)
        </div>
    }
</div>